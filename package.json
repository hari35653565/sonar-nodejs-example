{
  "name": "example1",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc -p .",
    "test": "jest --coverageDirectory test-coverage --coverage --watchAll=false",
    "test-first": "jest --collectCoverage",
    "test-coverage-firstj": "rm -rf test-coverage && jest --coverageDirectory test-coverage --collectCoverage --watchAll=false --testResultsProcessor jest-sonar",
    "test2": "jest --coverageDirectory test-coverage --coverage --watchAll=false",
    "test1": "jest --coverage",
    "test-coverage": "jest --coverage --watchAll",
    "test-coverage-first": "rm -rf test-coverage && jest --coverageDirectory test-coverage --coverage --watchAll=false --testResultsProcessor jest-sonar-reporter"
  },
  "author": "hari",
  "license": "ISC",
  "devDependencies": {
    "jest": "^21.2.1",
    "jest-sonar": "^0.2.16",
    "sonarqube-scanner": "^2.0.2",
    "ts-jest": "^21.1.3",
    "typescript": "^2.5.3"
  }
}
